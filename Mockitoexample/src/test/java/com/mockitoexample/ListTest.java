package com.mockitoexample;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import java.awt.List;

import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

class ListTest {

	/*@Test
	void testlist() {

		List listmock = mock(List.class);

		when(listmock.size()).thenReturn(10);

		assertEquals(10, listmock.size());
	}*/
	
	/*
	 * @Test void testspecific_value() {
	 * 
	 * List listmock = mock(List.class); when(listmock.get(0)).thenReturn("abcded");
	 * assertEquals("abcded",listmock.get(0)); assertEquals(null,listmock.get(1)); }
	 * 
	 * @Test void testgeneric_value() {
	 * 
	 * List listmock = mock(List.class);
	 * when(listmock.get(Mockito.anyInt())).thenReturn("abcded");
	 * assertEquals("abcded",listmock.get(0)); assertEquals("klki",listmock.get(1));
	 * }
	 */
	

}
